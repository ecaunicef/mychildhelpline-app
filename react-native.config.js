module.exports = {
    assets: ['./assets/fonts'],
    dependencies: {
        '@react-native-firebase/app': {
            platforms: {
                ios: null, // Mark this dependency as handled via Podfile
            },
        },
    },
}
